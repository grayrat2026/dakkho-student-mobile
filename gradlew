#!/bin/sh
# Gradle wrapper stub - full wrapper will be generated on first build
echo "Gradle wrapper not available in this environment."
echo "Run: gradle wrapper --gradle-version=8.11.1"
echo "Then: ./gradlew assembleDebug"
exit 1
